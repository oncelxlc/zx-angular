import { HeightObserverDirective } from './height-observer.directive';

describe('HeightObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightObserverDirective();
    expect(directive).toBeTruthy();
  });
});

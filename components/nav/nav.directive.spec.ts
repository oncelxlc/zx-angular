import { ZxNavDirective } from './nav.directive';

describe('NavDirective', () => {
  it('should create an instance', () => {
    const directive = new ZxNavDirective();
    expect(directive).toBeTruthy();
  });
});
